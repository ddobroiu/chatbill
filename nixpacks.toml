providers = ["..."]

[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = [
  "cd backend",
  "npm ci --only=production",
  "npx prisma generate"
]

[phases.build]
cmds = ["echo 'Build completed'"]

[start]
cmd = "cd backend && npm start"
